// components/Footer.js
const Footer = () => {
    return ( <
        footer style = { footerStyle } >
        <
        p > & copy; 2024 My Dummy App < /p> < /
        footer >
    );
};

const footerStyle = {
    background: '#333',
    color: '#fff',
    textAlign: 'center',
    padding: '1rem 0',
};

export default Footer;